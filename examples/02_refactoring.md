# 02 Refactoring

```bash
BarqCoder> Refactor src/main.rs to extract the HTTP logic into an `api` module.

# Planner outlines strategy
# Coder creates `src/api.rs` and modifies `main.rs`
# Reviewer checks module visibility
```
